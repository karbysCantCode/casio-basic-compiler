// This is a test program
#include "CASIOBASICCOMPILERHEADERS.h"
#include "testHeader.hpp"

int main() {
    int x = 42;
    float y = -3.14;
    float z = 0.5;
    float w = -0.25;

    string message = "Hello\nWorld!";
    char quote = '\'';

    x = x - 1;   // subtraction, not negative
    y = -x;      // negative identifier
    z--;         // decrement operator 

    if (x <= 100 && y != 0.0) {
        locate(1,1,message);
    }

    banana f;
    a = 10;
}

int add(int a, int b) {
  return a + b;
}

int the = {

};